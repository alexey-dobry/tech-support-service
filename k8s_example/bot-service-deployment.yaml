apiVersion: apps/v1
kind: Deployment
metadata:
  annotations:
    kompose.cmd: kompose convert -f docker-compose.yaml
    kompose.version: 1.34.0 (HEAD)
  labels:
    io.kompose.service: bot-service
  name: bot-service
spec:
  replicas: 1
  selector:
    matchLabels:
      io.kompose.service: bot-service
  template:
    metadata:
      annotations:
        kompose.cmd: kompose convert -f docker-compose.yaml
        kompose.version: 1.34.0 (HEAD)
      labels:
        io.kompose.service: bot-service
    spec:
      containers:
        - image: kvarz/bot-service:1.0.0
          name: bot-service
          ports:
            - containerPort: 8088
              protocol: TCP
      restartPolicy: Always
