apiVersion: apps/v1
kind: Deployment
metadata:
  annotations:
    kompose.cmd: kompose convert -f docker-compose.yaml
    kompose.version: 1.34.0 (HEAD)
  labels:
    io.kompose.service: session-service
  name: session-service
spec:
  replicas: 1
  selector:
    matchLabels:
      io.kompose.service: session-service
  template:
    metadata:
      annotations:
        kompose.cmd: kompose convert -f docker-compose.yaml
        kompose.version: 1.34.0 (HEAD)
      labels:
        io.kompose.service: session-service
    spec:
      containers:
        - image: kvarz/session-service:1.0.0
          name: session-service
          ports:
            - containerPort: 8070
              protocol: TCP
      restartPolicy: Always
